[user]
    name = FrontBack
    email = ndido98@gmail.com
[core]
    excludesfile = ~/.gitignore
    editor = vim
[push]
    default = simple
[alias]
    unstage = reset HEAD

# Specific configuration for GitHub
[url "git@github.com:"]
    # A pretty alias
    insteadof = "github:"

# Specific configuration for GitLab.com
[url "git@gitlab.com:"]
    # A pretty alias
    insteadof = "gitlab:"
